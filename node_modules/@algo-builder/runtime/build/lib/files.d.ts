import YAML from "yaml";
export declare function loadFromYamlFileSilent(filePath: string, options?: YAML.Options): any;
export declare function loadFromYamlFileSilentWithMessage(filePath: string, messageIfNotPresent: string, options?: YAML.Options): any;
/**
 * Reads the directory recursively and returns all paths
 * @param directoryName name of directory
 */
export declare function lsTreeWalk(directoryName: string): string[];
/**
 * Searches recursively and returns path of file in a given directory. Throws error
 * if multiple files with same name are found (in directory or sub-directory)
 * @param dir directory name
 * @param fileName name of file to search in directory
 * @param warnMsg if file does not exist & warning message is passed,
 * then console log warning, and return. throws error otherwise
 */
export declare function getPathFromDirRecursive(dir: string, fileName: string, warnMsg?: string): string | undefined;
