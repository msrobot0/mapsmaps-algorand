import { types } from "@algo-builder/web";
import { Interpreter } from "..";
import { Op } from "../interpreter/opcode";
import { EncTx, StackElem } from "../types";
/**
 * Sets inner transaction field to subTxn (eg. set assetReceiver('rcv'))
 * https://developer.algorand.org/docs/get-details/dapps/smart-contracts/apps/#setting-transaction-properties
 */
export declare function setInnerTxField(subTxn: EncTx, field: string, val: StackElem, op: Op, interpreter: Interpreter, line: number): EncTx;
export declare function parseEncodedTxnToExecParams(tx: EncTx, interpreter: Interpreter, line: number): types.ExecParams;
