import { AnyMap } from "../types";
import { ErrorDescriptor } from "./errors-list";
export declare const RUNTIME_ERROR_PREFIX = "RUNTIME_ERR";
export declare class RuntimeError extends Error {
    static isRuntimeError(other: any): other is RuntimeError;
    readonly errorDescriptor: ErrorDescriptor;
    readonly number: number;
    readonly parent?: Error;
    private readonly _isRuntimeError;
    constructor(errorDescriptor: ErrorDescriptor, messageArguments?: AnyMap, parentError?: Error);
}
