"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.installDependencies = exports.createConfirmationPrompt = exports.createProject = exports.printSuggestedCommands = exports.printWelcomeMessage = void 0;
const web_1 = require("@algo-builder/web");
const chalk_1 = __importDefault(require("chalk"));
const fs_extra_1 = __importDefault(require("fs-extra"));
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
const constants_1 = require("../constants");
const execution_mode_1 = require("../core/execution-mode");
const package_info_1 = require("../util/package-info");
const SAMPLE_PROJECT_DEPENDENCIES = [
    "chai"
];
const SAMPLE_TS_PROJECT_DEPENDENCIES = [
    ...SAMPLE_PROJECT_DEPENDENCIES, "@types/chai", "@types/node", "typescript", "ts-node"
];
async function printWelcomeMessage() {
    const packageJson = await (0, package_info_1.getPackageJson)();
    console.log(chalk_1.default.cyan(`★ Welcome to ${constants_1.ALGOB_NAME} v${packageJson.version}`));
}
exports.printWelcomeMessage = printWelcomeMessage;
function copy(directory, location) {
    fs_extra_1.default.copySync(directory, location, {
        // User doesn't choose the directory so overwrite should be avoided
        overwrite: false,
        filter: (src, dest) => {
            const relPath = path_1.default.relative(process.cwd(), dest);
            if (relPath === '') {
                return true;
            }
            if (path_1.default.basename(dest) === ".gitkeep") {
                return false;
            }
            return true;
        }
    });
}
function copySampleProject(location, isTSProject) {
    const packageRoot = (0, package_info_1.getPackageRoot)();
    const sampleProjDir = path_1.default.join(packageRoot, "sample-project");
    if (fs_extra_1.default.pathExistsSync(`./${location}/algob.config.js`)) {
        throw new web_1.BuilderError(web_1.ERRORS.GENERAL.INIT_INSIDE_PROJECT, {
            clashingFile: location
        });
    }
    console.log(chalk_1.default.greenBright("Initializing new workspace in " + path_1.default.join(process.cwd(), location)));
    // copy common files first
    copy(path_1.default.join(sampleProjDir, "common"), location);
    const projectDir = isTSProject ? path_1.default.join(sampleProjDir, "ts") : path_1.default.join(sampleProjDir, "js");
    // copy JS/TS project files, depending on --typescript flag
    copy(projectDir, location);
}
function printSuggestedCommands() {
    const npx = (0, execution_mode_1.getExecutionMode)() === execution_mode_1.ExecutionMode.EXECUTION_MODE_GLOBAL_INSTALLATION
        ? ""
        : "npx ";
    console.log(`Try running some of the following tasks:`);
    console.log(`  ${npx}${constants_1.ALGOB_NAME} gen-accounts`);
    console.log(`  ${npx}${constants_1.ALGOB_NAME} compile`);
    console.log(`  ${npx}${constants_1.ALGOB_NAME} test`);
    console.log(`  ${npx}${constants_1.ALGOB_NAME} node-info`);
    console.log(`  node scripts/sample-script.js`);
    console.log(`  ${npx}${constants_1.ALGOB_NAME} help`);
    console.log(`  ${npx}${constants_1.ALGOB_NAME} console`);
}
exports.printSuggestedCommands = printSuggestedCommands;
async function printPluginInstallationInstructions() {
    console.log(`You need to install these dependencies to run the sample project:`);
    const cmd = await npmInstallCmd();
    console.log(`  ${cmd.join(" ")}`);
}
async function createProject(location, isTSProject) {
    await printWelcomeMessage();
    copySampleProject(location, isTSProject);
    let shouldShowInstallationInstructions = true;
    const sampleProjectDependencies = isTypeScriptProject() ? SAMPLE_TS_PROJECT_DEPENDENCIES : SAMPLE_PROJECT_DEPENDENCIES;
    if (await canInstallPlugin()) {
        const installedRecommendedDeps = sampleProjectDependencies.filter(isInstalled);
        if (installedRecommendedDeps.length === sampleProjectDependencies.length) {
            shouldShowInstallationInstructions = false;
        }
        else if (installedRecommendedDeps.length === 0) {
            const shouldInstall = await confirmPluginInstallation();
            if (shouldInstall) {
                const installed = await installRecommendedDependencies();
                if (!installed) {
                    console.warn(chalk_1.default.red("Failed to install the sample project's dependencies"));
                }
                shouldShowInstallationInstructions = !installed;
            }
        }
    }
    console.log("\n★", chalk_1.default.cyan("Project created"), "★\n");
    if (shouldShowInstallationInstructions) {
        await printPluginInstallationInstructions();
        console.log(``);
    }
    printSuggestedCommands();
}
exports.createProject = createProject;
function createConfirmationPrompt(name, message) {
    return {
        type: "confirm",
        name,
        message,
        initial: "y",
        default: "(Y/n)",
        isTrue(input) {
            if (typeof input === "string") {
                return input.toLowerCase() === "y";
            }
            return input;
        },
        isFalse(input) {
            if (typeof input === "string") {
                return input.toLowerCase() === "n";
            }
            return input;
        },
        format() {
            const that = this; // eslint-disable-line @typescript-eslint/no-explicit-any
            const value = that.value === true ? "y" : "n";
            if (that.state.submitted === true) {
                return that.styles.submitted(value);
            }
            return value;
        }
    };
}
exports.createConfirmationPrompt = createConfirmationPrompt;
async function canInstallPlugin() {
    return ((await fs_extra_1.default.pathExists("package.json")) &&
        ((0, execution_mode_1.getExecutionMode)() === execution_mode_1.ExecutionMode.EXECUTION_MODE_LOCAL_INSTALLATION ||
            (0, execution_mode_1.getExecutionMode)() === execution_mode_1.ExecutionMode.EXECUTION_MODE_LINKED) &&
        // TODO: Figure out why this doesn't work on Win
        os_1.default.type() !== "Windows_NT");
}
function isInstalled(dep) {
    const packageJson = fs_extra_1.default.readJSONSync("package.json");
    const allDependencies = {
        ...packageJson.dependencies,
        ...packageJson.devDependencies,
        ...packageJson.optionalDependencies
    };
    return dep in allDependencies;
}
function isYarnProject() {
    return fs_extra_1.default.pathExistsSync("yarn.lock");
}
function isTypeScriptProject() {
    return fs_extra_1.default.pathExistsSync("tsconfig.json");
}
async function installRecommendedDependencies() {
    console.log("");
    const installCmd = await npmInstallCmd();
    return await installDependencies(installCmd[0], installCmd.slice(1));
}
async function confirmPluginInstallation() {
    const { default: enquirer } = await Promise.resolve().then(() => __importStar(require("enquirer")));
    let responses;
    const packageManager = isYarnProject() ? "yarn" : "npm";
    const sampleProjectDependencies = isTypeScriptProject() ? SAMPLE_TS_PROJECT_DEPENDENCIES : SAMPLE_PROJECT_DEPENDENCIES;
    try {
        responses = await enquirer.prompt([
            createConfirmationPrompt("shouldInstallPlugin", `Do you want to install the sample project's dependencies with ${packageManager} (${sampleProjectDependencies.join(" ")})?`)
        ]);
    }
    catch (e) {
        if (e === "") {
            return false;
        }
        throw e;
    }
    return responses.shouldInstallPlugin;
}
async function installDependencies(packageManager, args, location) {
    const { spawn } = await Promise.resolve().then(() => __importStar(require("child_process")));
    console.log(`${packageManager} ${args.join(" ")}`);
    const childProcess = spawn(packageManager, args, {
        stdio: "inherit",
        cwd: location
    });
    return await new Promise((resolve, reject) => {
        childProcess.once("close", (status) => {
            childProcess.removeAllListeners("error");
            if (status === 0) {
                resolve(true);
                return;
            }
            reject(new Error("script process returned not 0 status"));
        });
        childProcess.once("error", (status) => {
            childProcess.removeAllListeners("close");
            reject(new Error("script process returned not 0 status"));
        });
    });
}
exports.installDependencies = installDependencies;
async function npmInstallCmd() {
    const isGlobal = (0, execution_mode_1.getExecutionMode)() === execution_mode_1.ExecutionMode.EXECUTION_MODE_GLOBAL_INSTALLATION;
    const sampleProjectDependencies = isTypeScriptProject() ? SAMPLE_TS_PROJECT_DEPENDENCIES : SAMPLE_PROJECT_DEPENDENCIES;
    if (isYarnProject()) {
        const cmd = ["yarn"];
        if (isGlobal) {
            cmd.push("global");
        }
        cmd.push("add", "--dev", ...sampleProjectDependencies);
        return cmd;
    }
    const npmInstall = ["npm", "install"];
    if (isGlobal) {
        npmInstall.push("--global");
    }
    return [...npmInstall, "--save-dev", ...sampleProjectDependencies];
}
//# sourceMappingURL=project-creation.js.map